{
  "buildCommand": "npx nx build webapp --prod --skip-nx-cache",
  "outputDirectory": "apps/webapp/dist",
  "framework": "vite",
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/",
      "status": 200
    }
  ]
}
